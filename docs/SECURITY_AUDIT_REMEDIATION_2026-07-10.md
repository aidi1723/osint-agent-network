# Security Audit Remediation Ledger

## Scope and usage

This ledger tracks the eight findings accepted in the 2026-07-10 security-hardening design. Status lifecycle: `planned` = no implementation/targeted green evidence; `implemented` = targeted regression is green but full verification incomplete; `verified` = full repository verification and final evidence captured. Update the relevant row immediately after its targeted regression test turns green, and close a finding only after the complete verification suite has passed.

| ID | Severity | Original reproduction / evidence | Target regression test | Implementation status | Verification evidence | Residual risk |
| --- | --- | --- | --- | --- | --- | --- |
| SEC-01 | High | Baseline reproduction: built with `VITE_ADMIN_API_TOKEN=<audit-canary>`; `rg` found `<audit-canary>` in `dist/assets/*.js`. The browser token path was at `frontend/src/main.tsx:35`, and `docs/PROJECT_PACKAGE.md:118` documents the former long-lived management token configuration. | Frontend auth/build regression: assert browser code constructs no Authorization header from environment tokens and scan production assets for credential values. | verified | Implemented in `072267c` through `11f6c11`: 61 backend browser-auth/integration tests and 29 frontend tests passed; the Chinese UI check and canary production build passed; `rg -n '<audit-canary>' frontend/dist` returned no matches. Independent specification and quality reviews approved Tasks 2 and 3 with no remaining findings. | In-memory sessions intentionally require login again after an API restart. Production operation still depends on correct TLS termination, secure Cookie configuration, and protected runtime credentials. |
| SEC-02 | High | Baseline reproduction: a completion `POST` using `agent_id=unregistered-attacker` was accepted and changed the task summary/report. Relevant handling and mutation sites are `backend/app/main.py:204`, `backend/app/main.py:547`, and `backend/app/services/store.py:1727`. | Backend agent authorization matrix: cover token hashing and rotation, forged identity, wrong role, unregistered agent, claim ownership, and legacy-mode production rejection. | verified | Implemented per-agent credentials, exact route roles, atomic claims and scoped mutations, atomic tool output, and administrator-controlled registration across `fe09f11`, `498b9b1`, `1d170e1`, `b33629e`, `fc08b17`, `1b138c9`, `479253d`, `ad3fa2d`, `7d1c4ad`, `4ead964`, `b4783eb`, `d616ead`, and `160f17c`. Final targeted Task 5 review passed 179/179 tests and the full backend passed 576/576; independent specification and quality reviews approved the implementation with no remaining findings. | SQLite large-report write-lock latency remains an operational scaling risk. |
| SEC-03 | High | Baseline reproduction: `http://127.1:8765/` normalized successfully and was fetched with HTTP status 200. The permissive normalization and fetch paths are `backend/app/core/normalization.py:13` and `backend/app/tools/official_site_extractor.py:95`. | Safe-fetch regression suite: cover IPv4/IPv6 private ranges, alternate loopback forms, mixed DNS answers, redirect hops, response limits, and an injected public-safe connection. | verified | Implemented safe fetch, canonical and legacy literal classification, bounded DNS validation, redirect revalidation, pinned-IP connections with original Host/SNI, response and redirect limits, one total deadline, bounded gzip expansion, and non-reflective failures across `b91e343`, `903111b`, `3d258f2`, `f968a78`, `e545957`, and `1bea5e6`. Final focused combined regression passed 141/141 tests and the full backend passed 620/620; independent specification and quality reviews approved the implementation, and real local HTTP and TLS probes passed. | Resolver-thread behavior in pre-fork deployments, bounded concatenated-gzip CPU cost, and the absence of a committed end-to-end TLS integration test remain residual risks. |
| REL-01 | Medium | Baseline evidence: the personal `/home/aidi/...` path existed at `PROJECT_CLOSURE_MAINTENANCE_LOG.md:4`; release policy classifies personal paths as blockers, but `scripts/public_release_check.py` returned ready because it checked only licenses. | Release-policy fixture suite: cover personal paths, private/LAN IPs, secrets, runtime artifacts, licenses, allowed placeholders, and filesystem fallback behavior. | verified | Implemented deterministic tracked-file and fallback discovery, non-dereferenced symlink scanning, runtime-artifact checks, cross-platform personal-path detection, bounded AST-based Python/JavaScript/TypeScript credential analysis, exact-path fixture exceptions, stable non-reflective failures, and end-to-end resource limits across `5c046c1` through `c136256`. Final targeted evidence passed 91/91 scanner tests, 294/294 Task 7 contract tests, and 731/731 backend tests; the repository self-scan returned ready with zero findings, and independent specification and quality reviews approved the result. | The user-modified maintenance log remains intentionally uncommitted after personal paths were replaced with `<production-path>`. The scanner intentionally fails closed when its Node/TypeScript parser prerequisite is unavailable. |
| REL-02 | Medium | Baseline evidence: `backend/pyproject.toml:5` declared `Proprietary`, while `README.md`, frontend package metadata, and `LICENSE` declared `GPL-3.0-only`; the public release checker did not inspect backend package metadata. | License consistency regression: assert `LICENSE`, frontend metadata, and backend metadata all declare `GPL-3.0-only`. | verified | Backend metadata now declares `GPL-3.0-only`; the release gate validates the canonical `LICENSE`, frontend package metadata, and backend package metadata. License mismatch, malformed declaration, and repository-level release tests pass, and the final Task 7 self-scan reports all three declarations as GPL-3.0-only with zero blockers. The licensing change was introduced in `9233486`; independent specification and quality reviews approved the complete release-gate chain through `c136256`. | Future metadata changes could reintroduce license drift; the release gate must remain mandatory. |
| DEP-01 | Medium | Baseline evidence: a full npm audit against the official registry returned 2 high and 2 low findings: `GHSA-v6wh-96g9-6wx3`, `GHSA-fx2h-pf6j-xcff`, `GHSA-vmh5-mc38-953g`, and `GHSA-vxpw-j846-p89q`. npm also summarized transitive moderate/low entries under the affected packages. | Dependency gate: run production-only and full npm audits and require zero high or critical findings. | verified | Updated Vite to 8.1.4, `@vitejs/plugin-react` to 6.0.3, and the compatible transitive `undici` lock entry to 7.28.0 in `fc3a099` without `--force` or unrelated direct dependency changes. Independent official-registry production and full audits both reported zero vulnerabilities; 29 frontend tests, the Chinese UI-copy check, the Vite production build, clean-install dry run, dependency-tree validation, and public self-scan passed. Independent specification and quality reviews approved the dependency update. | Future lockfile refreshes must retain official-registry integrity metadata and the documented Node engine floor. |
| RTR-01 | Medium | Baseline reproduction: the original Chinese request `审核当前目录下的软件项目，检查代码质量、正确性、安全性、配置、测试、构建与可维护性` selected `website-build-launch` instead of code review. | The fix belongs to the separate `<safe-agent-skills-repository>`; add router regressions in `tests/test_router.py` and evaluation cases in `evals/router-quality.json` covering the original audit request, shorter Chinese variants, and ambiguous non-code-review controls. | verified | Implemented on the isolated `safe-agent-skills` branch through `55a6c5b`: code-review-specific Chinese signals and an audit-intent-plus-directory-software-scope compound rule handle exact, spaced, punctuated, and reverse-order prompts without changing global scoring or tie-breaking. Router evaluation passed 40/40, full repository verification passed 91/91, real smart probes remained trusted-only, and UI, social-copy, supplier-report, bare-review, packaging, analysis, and website-build controls retained their expected classifications. Independent specification and quality reviews approved the result. | Future signal additions must retain explicit non-review controls and avoid bare audit or project terms. |
| UI-01 | Medium | Baseline visual evidence: local ignored desktop `.playwright-cli/page-2026-07-10T03-46-10-868Z.png` and mobile `.playwright-cli/page-2026-07-10T03-46-35-198Z.png` artifacts showed only the active-task detail overlay and label overflow, not the empty state. Durable code evidence for forced default detail is `frontend/src/components/HcsTemplateGraph.tsx:101`, the always-rendered aside is at `frontend/src/components/HcsTemplateGraph.tsx:335`, and its absolute overlay is at `frontend/src/styles.css:2676`. Separate empty-state code evidence is `frontend/src/main.tsx:564`, which renders only “点击任务池中的任一任务查看数据看板”, while the operations panel begins as a closed `<details>` at `frontend/src/main.tsx:568`. | Frontend graph/empty-state tests plus Playwright desktop and mobile assertions for details, labels, responsiveness, accessibility, console errors, and overflow. | verified | Implemented through `60a52dc`: 45/45 frontend tests, responsive CSS checks, Chinese UI-copy checks, the Vite production build, and the public self-scan passed. Playwright at 1440x1000 and 390x844 confirmed an actionable empty state; initially closed, keyboard-selectable graph details; `aria-pressed` selection; focus return to the triggering SVG node after close; 11/11 bounded node labels with full SVG titles; a non-overlapping sticky desktop detail; a static mobile detail after a 700px-wide horizontally scrollable graph viewport; zero document horizontal overflow; and zero console errors or warnings. Independent specification and quality reviews approved the implementation. | The no-`Intl.Segmenter` fallback intentionally replaces over-limit non-ASCII display labels with an ellipsis while preserving the full SVG title. |

## Targeted regression index

These commands are the targeted green gates used to move each finding to `implemented`. Task 12 reruns the complete cross-repository gates before any status becomes `verified`.

| ID | Regression test / gate | Implementation commit(s) | Targeted green command |
| --- | --- | --- | --- |
| SEC-01 | `frontend/src/auth.test.ts`, `backend.tests.test_browser_auth`, and browser-auth routes in `backend.tests.test_agent_auth` | `072267c` through `11f6c11` | `PYTHONPATH=backend backend/.venv/bin/python -m unittest backend.tests.test_browser_auth backend.tests.test_agent_auth -v && npm --prefix frontend test -- src/auth.test.ts` |
| SEC-02 | `backend.tests.test_agent_identity_auth`, `backend.tests.test_agent_protocol`, `backend.tests.test_agent_client`, and `backend.tests.test_store_dedup` | `fe09f11` through `160f17c` | `PYTHONPATH=backend backend/.venv/bin/python -m unittest backend.tests.test_agent_identity_auth backend.tests.test_agent_protocol backend.tests.test_agent_client backend.tests.test_store_dedup -v` |
| SEC-03 | `backend.tests.test_safe_http`, plus normalization and adapter integration cases in `backend.tests.test_core` and `backend.tests.test_tool_adapters` | `b91e343` through `1bea5e6` | `PYTHONPATH=backend backend/.venv/bin/python -m unittest backend.tests.test_safe_http backend.tests.test_core backend.tests.test_tool_adapters -v` |
| REL-01 | `backend.tests.test_public_release_check` scanner fixtures and repository self-scan | `5c046c1` through `c136256` | `PYTHONPATH=backend backend/.venv/bin/python -m unittest backend.tests.test_public_release_check -v && python3 scripts/public_release_check.py` |
| REL-02 | GPL consistency cases in `backend.tests.test_public_release_check` and `backend.tests.test_release_artifacts` | `9233486` | `PYTHONPATH=backend backend/.venv/bin/python -m unittest backend.tests.test_public_release_check backend.tests.test_release_artifacts -v` |
| DEP-01 | Official-registry production and full dependency audit gates | `fc3a099` | `npm --prefix frontend audit --omit=dev --registry=https://registry.npmjs.org && npm --prefix frontend audit --registry=https://registry.npmjs.org` |
| RTR-01 | `<safe-agent-skills-repository>/tests/test_router.py` and `evals/router-quality.json` Chinese audit/control cases | `55a6c5b` | `PYTHONPATH=src python3 -m onecode_skill_sanitizer router-eval --eval evals/router-quality.json --registry catalog --bundles bundles/index.json && bash scripts/verify.sh` (run in the `safe-agent-skills` repository) |
| UI-01 | `frontend/src/components/HcsTemplateGraph.test.tsx`, `frontend/src/graph-labels.test.ts`, `frontend/src/main.test.tsx`, and `frontend/scripts/test-responsive-css.ts` | `5608842` through `60a52dc` | `npm --prefix frontend test && node --experimental-strip-types frontend/scripts/test-responsive-css.ts && npm --prefix frontend run check:ui-copy && npm --prefix frontend run build` |

UI-01 browser evidence was captured from a temporary SQLite database with the API at `http://127.0.0.1:18088` and Vite at `http://127.0.0.1:13008`. Durable screenshots are stored under `output/playwright/task10-empty-desktop.png`, `output/playwright/task10-graph-desktop.png`, `output/playwright/task10-graph-mobile-empty.png`, and `output/playwright/task10-graph-mobile-detail.png`; these artifacts are verification evidence and are not part of the release commit.

## Final Task 12 verification evidence

All eight rows reached `verified` only after the following fresh checks completed on 2026-07-11:

- `bash scripts/verify.sh` passed both before and after integration in `osint-agent-network`: 731/731 backend tests, 4/4 regression-smoke cases, all frontend helper checks, 45/45 Vitest tests, and the Vite production build passed.
- `bash scripts/verify.sh` passed both before and after integration in `safe-agent-skills`: 91/91 tests plus maintain-check, reference-check, 40/40 router evaluation, schema checks, trusted smart-routing probes, JSON validation, and repository residue checks passed.
- After `npm install` synchronized the original workspace to Vite 8.1.4, `@vitejs/plugin-react` 6.0.3, and `undici` 7.28.0, `npm test` passed 45/45 and `npm run build` used Vite 8.1.4. Official-registry `npm audit --omit=dev --json --registry=https://registry.npmjs.org` and full `npm audit --json --registry=https://registry.npmjs.org` each reported 0 total vulnerabilities across all severity levels.
- Four focused Agent HTTP reproductions passed: exact role/action enforcement, forged identity rejection, active ownership enforcement, and unregistered completion rejection. Four focused safe-fetch reproductions passed: private literals including alternate IPv4/IPv6 forms, mixed private/public DNS rejection, redirect-to-private rejection, and no second connection for a private literal redirect.
- `env 'VITE_ADMIN_API_TOKEN=<audit-canary>' npm run build` succeeded, and `rg -n '<audit-canary>' dist` returned no matches.
- Final Playwright checks used 1440x1000 and 390x844. Desktop confirmed 11/11 button nodes and 11/11 full titles, `aria-pressed=true`, a non-overlapping sticky detail, and zero document overflow. Mobile confirmed a static detail after a 700px horizontally scrollable graph viewport, zero document overflow, and retained selection. The console reported 0 errors and 0 warnings. Final screenshots are `output/playwright/task12-final-desktop.png` and `output/playwright/task12-final-mobile.png` in the isolated OSINT worktree.
- `security-hardening-router` was fast-forwarded to `55a6c5b`, and `security-hardening` was fast-forwarded through `900bc58`. The user's `safe-agent-skills/.gitignore` additions remain uncommitted and unchanged. The user's OSINT maintenance-log edits remain uncommitted; only personal paths were replaced with `<production-path>` placeholders.
