# Security Audit Remediation Ledger

## Scope and usage

This ledger tracks the eight findings accepted in the 2026-07-10 security-hardening design. Status lifecycle: `planned` = no implementation/targeted green evidence; `implemented` = targeted regression is green but full verification incomplete; `verified` = full repository verification and final evidence captured. Update the relevant row immediately after its targeted regression test turns green, and close a finding only after the complete verification suite has passed.

| ID | Severity | Original reproduction / evidence | Target regression test | Implementation status | Verification evidence | Residual risk |
| --- | --- | --- | --- | --- | --- | --- |
| SEC-01 | High | Baseline reproduction: built with `VITE_ADMIN_API_TOKEN=audit-canary-admin-token`; `rg` found `audit-canary-admin-token` in `dist/assets/*.js`. The browser token path is at `frontend/src/main.tsx:35`, and `docs/PROJECT_PACKAGE.md:118` documents the long-lived management token configuration. | Frontend auth/build regression: assert browser code constructs no bearer header from environment tokens and scan production assets for credential values. | planned | Planned: targeted frontend auth test plus production build asset scan; not run yet. | Browser credential exposure remains until cookie-based admin sessions replace environment-token access and the asset canary passes. |
| SEC-02 | High | Baseline reproduction: a completion `POST` using `agent_id=unregistered-attacker` was accepted and changed the task summary/report. Relevant handling and mutation sites are `backend/app/main.py:204`, `backend/app/main.py:547`, and `backend/app/services/store.py:1727`. | Backend agent authorization matrix: cover token hashing and rotation, forged identity, wrong role, unregistered agent, claim ownership, and legacy-mode production rejection. | planned | Planned: targeted backend agent authentication/authorization tests; not run yet. | Cross-agent impersonation and unauthorized task mutation remain possible until per-agent credentials and all route/ownership checks are verified. |
| SEC-03 | High | Baseline reproduction: `http://127.1:8765/` normalized successfully and was fetched with HTTP status 200. The permissive normalization and fetch paths are `backend/app/core/normalization.py:13` and `backend/app/tools/official_site_extractor.py:95`. | Safe-fetch regression suite: cover IPv4/IPv6 private ranges, alternate loopback forms, mixed DNS answers, redirect hops, response limits, and an injected public-safe connection. | planned | Planned: targeted safe-fetch and target-normalization tests with injected resolver/connector fakes; not run yet. | Private-network access or redirect-based policy bypass remains possible until DNS pinning, redirect validation, and limits are verified. |
| REL-01 | Medium | Baseline evidence: the personal `/home/aidi/...` path existed at `PROJECT_CLOSURE_MAINTENANCE_LOG.md:4`; release policy classifies personal paths as blockers, but `scripts/public_release_check.py` returned ready because it checked only licenses. | Release-policy fixture suite: cover personal paths, private/LAN IPs, secrets, runtime artifacts, licenses, allowed placeholders, and filesystem fallback behavior. | planned | Planned: targeted `public_release_check.py` tests and the repository release check; not run yet. | A release may still publish policy-blocking content until all scanner rules and repository-level checks pass. |
| REL-02 | Medium | Baseline evidence: `backend/pyproject.toml:5` declared `Proprietary`, while `README.md`, frontend package metadata, and `LICENSE` declared `GPL-3.0-only`; the public release checker did not inspect backend package metadata. | License consistency regression: assert `LICENSE`, frontend metadata, and backend metadata all declare `GPL-3.0-only`. | planned | Planned: targeted release-policy license fixture and repository release check; not run yet. | Conflicting licensing metadata remains until declarations are aligned and the consistency gate passes. |
| DEP-01 | Medium | Baseline evidence: a full npm audit against the official registry returned 2 high and 2 low findings: `GHSA-v6wh-96g9-6wx3`, `GHSA-fx2h-pf6j-xcff`, `GHSA-vmh5-mc38-953g`, and `GHSA-vxpw-j846-p89q`. npm also summarized transitive moderate/low entries under the affected packages. | Dependency gate: run production-only and full npm audits and require zero high or critical findings. | planned | Planned: `npm audit --omit=dev` and `npm audit`; not run yet. | Known dependency advisories remain until lockfile updates are installed, tested, and both audits pass against the official registry. |
| RTR-01 | Medium | Baseline reproduction: the original Chinese request `审核当前目录下的软件项目，检查代码质量、正确性、安全性、配置、测试、构建与可维护性` selected `website-build-launch` instead of code review. | The fix belongs to the separate repository `/Users/aidi/情报官/safe-agent-skills`; add router regressions in `tests/test_router.py` and evaluation cases in `evals/router-quality.json` covering the original audit request, shorter Chinese variants, and ambiguous non-code-review controls. | planned | Planned, not run yet: `PYTHONPATH=src python3 -m onecode_skill_sanitizer router-eval --eval evals/router-quality.json --registry catalog --bundles bundles/index.json` from `/Users/aidi/情报官/safe-agent-skills`. | Chinese code-audit intent may continue to misroute until specific compound signals pass without changing existing classifications. |
| UI-01 | Medium | Baseline visual evidence: local ignored desktop `.playwright-cli/page-2026-07-10T03-46-10-868Z.png` and mobile `.playwright-cli/page-2026-07-10T03-46-35-198Z.png` artifacts showed only the active-task detail overlay and label overflow, not the empty state. Durable code evidence for forced default detail is `frontend/src/components/HcsTemplateGraph.tsx:101`, the always-rendered aside is at `frontend/src/components/HcsTemplateGraph.tsx:335`, and its absolute overlay is at `frontend/src/styles.css:2676`. Separate empty-state code evidence is `frontend/src/main.tsx:564`, which renders only “点击任务池中的任一任务查看数据看板”, while the operations panel begins as a closed `<details>` at `frontend/src/main.tsx:568`. | Frontend graph/empty-state tests plus Playwright desktop and mobile assertions for details, labels, responsiveness, accessibility, console errors, and overflow. | planned | Planned: targeted frontend UI tests and Playwright desktop/mobile verification; not run yet. | Graph readability, keyboard access, responsive behavior, and empty-state usability remain unverified until automated and visual checks pass. |

UI-01 reproducible baseline (planned, not rerun): from the repository root, start the API with `OSINT_DB_PATH="$(mktemp -d)/osint.sqlite" PYTHONPATH=backend python3 -m app.main` and the frontend with `npm --prefix frontend run dev`. In Playwright, load the Vite URL at `1440x1000` and `390x844`, capture the fresh-database empty state, then create/select a task and capture the active-task graph; assert detail visibility/toggle behavior, node-label bounds, and document overflow.
